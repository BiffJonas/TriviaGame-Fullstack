{
	"name": "client",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "concurrently \"npm:watch\" \"npm:dev\"",
		"watch": "tsc -w",
		"dev": "lite-server"
	},
	"lite": {
  "config": {
    "baseDir": "./public"
  }
},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.2.2",
		"typescript": "^5.3.3"
	},
	"dependencies": {
		"bootstrap": "^5.3.2",
		"lite-server": "^2.6.1"
	}
}
